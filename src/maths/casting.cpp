#include "casting.h"

namespace voxmt {
    glm::vec2 uintBitsToFloat2(unsigned int value) {
        return glm::vec2(0);
    }

    unsigned int float2BitsToUInt(const glm::vec2& value) {
        return 0u;
    }
}
