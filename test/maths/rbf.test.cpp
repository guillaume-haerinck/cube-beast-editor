#include <catch2/catch.hpp>

#include "maths/rbf.h"

SCENARIO("Explain", "[map-generation]") {
    GIVEN("Blabla") {

        WHEN("blabla") {

            THEN("blabla") {
                REQUIRE(true);
            }
        }
    }
}
